.next
node_modules
.env
.env*.local
dist
*.log

# Cursor / local tooling (do not publish)
agent-transcripts/
terminals/
mcps/

# Local uploads / reference images (add your own)
assets/

# Accidental local installers (do not commit)
*.exe
